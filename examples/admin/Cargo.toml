[package]
name = "example-admin"
version = "0.1.0"
publish = false
description = "Admin panel - Cot example."
edition = "2021"

[dependencies]
askama = "0.13"
cot = { path = "../../cot", features = ["live-reload"] }
